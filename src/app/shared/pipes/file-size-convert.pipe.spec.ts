import { FileSizeConvertPipe } from './file-size-convert.pipe';

describe('FileSizeConvertPipe', () => {
  it('create an instance', () => {
    const pipe = new FileSizeConvertPipe();
    expect(pipe).toBeTruthy();
  });
});
