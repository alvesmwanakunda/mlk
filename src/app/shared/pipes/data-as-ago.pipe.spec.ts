import { DataAsAgoPipe } from './data-as-ago.pipe';

describe('DataAsAgoPipe', () => {
  it('create an instance', () => {
    const pipe = new DataAsAgoPipe();
    expect(pipe).toBeTruthy();
  });
});
